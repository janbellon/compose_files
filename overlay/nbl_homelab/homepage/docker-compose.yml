include:
  - ../../../base/homepage/compose.yml

services:
  homepage:
    labels:
      caddy: ${DOMAIN}
      caddy.reverse_proxy: "http://homepage:3000"
      caddy.tls: /certs/${CERT_DOMAIN}/fullchain.pem /certs/${CERT_DOMAIN}/privkey.pem
    env_file:
      - path: override.env
        required: true
    networks:
      - caddy
    volumes:
      - ./priv_config:/app/config
      
networks:
  caddy:
    external: true
