services:
  vaultwarden:
    image: vaultwarden/server:1.37.3
    container_name: vaultwarden
    restart: unless-stopped
    env_file:
      - path: default.env
        required: true
    volumes:
      - vw-data:/data

volumes:
  vw-data:
